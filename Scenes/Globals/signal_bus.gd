extends Node
# SignalBus.gd

# We define the signal name, and what data it carries (in this case, the object itself)
signal shape_landed(shape) 
signal shape_cleared(shape) 
signal roof_cracked(pressure_level)
